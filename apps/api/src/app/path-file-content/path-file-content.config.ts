/** Maximum bytes read for text preview (memory-bounded). */
export const FILE_PREVIEW_MAX_BYTES = 512 * 1024;

/** Bytes scanned for NUL to detect binary content. */
export const BINARY_SNIFF_LENGTH = 8192;
